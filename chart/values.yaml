# Copyright 2024 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

domain: "###ZARF_VAR_DOMAIN###"

clustering:
  enabled: false

postgres:
  # Set to false to use external postgres
  internal: true
  selector:
    cluster-name: pg-cluster
  username: confluence
  password: ""
  namespace: postgres
  port: 5432

setup:
  instanceDisplayName: Confluence
  generateRandomAdminPassword:
    # Set to false to provide your own initial admin secret
    enabled: true
    username: admin
    email: admin@uds.dev
    displayName: Instance Admin
  license:
    # Set to false to provide your own license
    generateSecret: true
    licenseKeyData: ""

sso:
  enabled: true
  # Options: "saml"
  protocol: saml
  groups:
    - /uds core
    - /uds core/admin
    - /uds core/auditor
  groupPermissions:
    - groupName: /uds core/admin
      permission: SYS_ADMIN
    - groupName: /uds core
      permission: LICENSED_USER
  attributes:
    username: name
    firstname: first_name
    lastname: last_name
    email: email
    groups: groups

# custom:
#    # Notice no `remoteGenerated` field here on custom internal rule
#   - direction: Ingress
#     selector:
#       app: jenkins
#     remoteNamespace: jenkins
#     remoteSelector:
#       app: jenkins
#     port: 8180
#     description: "Ingress from Jenkins"
#   # No `remoteNamespace`, `remoteSelector`, or `port` fields on rule to `remoteGenerated`
#   - direction: Egress
#     selector:
#       app: webservice
#     remoteGenerated: Anywhere
#     description: "Egress from Mattermost"
